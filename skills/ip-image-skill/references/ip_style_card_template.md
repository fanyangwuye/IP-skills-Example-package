# IP Style Card Template

Use one card per IP or visual line.

Suggested fields:

- Style direction
- Primary palette
- Character anchors
- Forbidden elements
- Positive prompt fragments
- Negative prompt fragments

The agent or caller can combine these fields into the final prompt before generation.

